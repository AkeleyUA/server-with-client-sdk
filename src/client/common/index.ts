export const PROTOCOL = 'http';
export const ID = '192.168.0.102';
export const PORT = 4000;
export const DOMAIN = `${ID}:${PORT}`;
export const API_PREFIX = 'api';

export const BASE_URL = `${PROTOCOL}://${DOMAIN}/${API_PREFIX}`;
